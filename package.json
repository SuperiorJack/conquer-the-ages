{
  "name": "Conquer-The",
  "license": "MIT",
  "engines": {
    "node": "11.4.0",
    "yarn": "1.12.3"
  },
  "scripts": {
    "build": "yarn --cwd client install && ./client/node_modules/.bin/tailwind build ./client/src/styles/fuse-helpers.tailwind.css -c ./client/tailwind.js -o ./client/src/styles/fuse-helpers.css && ./client/node_modules/.bin/postcss --use autoprefixer -o ./client/src/styles/fuse-helpers.css ./client/src/styles/fuse-helpers.css && yarn --cwd client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}